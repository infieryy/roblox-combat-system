--!strict
-- Module: shared/Config.luau

local Config = {
	Drag = {
		maxPickupDistance = 12,
		serverDistanceTolerance = 4,
		maxHoldDistance = 14,
		leashGracePeriod = 0.6,
		leashInterval = 0.2,
		maxObjectMass = 5000,
		pivotCorrectionThreshold = 0.2,
		positionResponsiveness = 55,
		forceMultiplier = 50,
		minForce = 30000,
		maxForce = 8_000_000,
		maxVelocity = 90,
		orientationResponsiveness = 50,
		torqueMultiplier = 4000,
		minTorque = 40000,
		maxTorque = 8_000_000,
		maxAngularVelocity = 35,
		dropImpulseSpeed = 6,
		holdDistanceBase = 4,
		holdDistancePadding = 1.0,
		holdDistanceMin = 3,
		holdDistanceMax = 10,
		scrollStep = 0.75,
		enableInspect = false,
		rotateSensitivity = 0.0125,
		flingVelocityMultiplier = 1.0,
	},
	Viewmodel = {
		swayAmount = 0.04,
		swaySpeed = 8,
		bobAmount = 0.03,
		bobSpeed = 12,
		baseOffsetCFrame = CFrame.new(0.6, -0.65, -1.1),
		springDamping = 0.75,
		springFrequency = 10,
		fireKickAmount = 0.08,
		fireKickRecovery = 14,
	},
	Weapon = {
		serverRayLengthPadding = 4,
		maxAimDeviationDegrees = 12,
		tracerLifetime = 0.08,
	},
	Security = {
		fireRequestCooldownMs = 30,
		dragRequestCooldownMs = 150,
		equipRequestCooldownMs = 250,
		reloadRequestCooldownMs = 500,
		maxEquipDistance = 14,
		maxDragStartDistance = 16,
	},
	CollisionGroups = {
		heldObjects = "PickupHeldObjects",
		characters = "PickupCharacters",
		default = "Default",
	},
	RemotesFolderName = "ItemSystemRemotes",
	HeldByAttribute = "PickupHeldBy",
	LegacyTag = "Interactable",
	LegacyAttribute = "Interactable",
	Input = {
		equipKey = Enum.KeyCode.E,
		reloadKey = Enum.KeyCode.R,
		gamepadEquipKey = Enum.KeyCode.ButtonR1,
	},
	ViewmodelsFolderName = "Viewmodels",
}

return Config
